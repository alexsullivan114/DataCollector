<?xml version="1.0" encoding="utf-8"?>
<shape android:shape="rectangle" xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/ratingPoor" />
    <stroke android:color="@android:color/white" android:width="@dimen/ratingStrokeWidth"/>
    <corners
        android:bottomLeftRadius="@dimen/ratingCorners"
        android:bottomRightRadius="@dimen/ratingCorners"
        android:topLeftRadius="@dimen/ratingCorners"
        android:topRightRadius="@dimen/ratingCorners" />
</shape>
